import 'package:flutter/material.dart';

// ----------------------------
// Home Placeholder
// ----------------------------
class PlaceholderHomePage extends StatelessWidget {
  const PlaceholderHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Home Screen",
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}

// ----------------------------
// Submit Complaint Placeholder
// ----------------------------
class PlaceholderSubmitComplaint extends StatelessWidget {
  const PlaceholderSubmitComplaint({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Submit Complaint",
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}

// ----------------------------
// Tracking Placeholder
// ----------------------------
class PlaceholderTrackingPage extends StatelessWidget {
  const PlaceholderTrackingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Complaint Tracking",
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}

// ----------------------------
// Community Placeholder
// ----------------------------
class PlaceholderCommunityPage extends StatelessWidget {
  const PlaceholderCommunityPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Community Page",
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}

// ----------------------------
// Profile Placeholder
// ----------------------------
class PlaceholderProfilePage extends StatelessWidget {
  const PlaceholderProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Profile",
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}
